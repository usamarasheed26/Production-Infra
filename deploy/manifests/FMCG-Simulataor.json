{
    "repo":  "usamarasheed26/FMCG-Simulataor",
    "branch":  "feature/stratagem-integration-and-spec-consolidation",
    "compose":  [
                    "-f",
                    "docker-compose.prod.yml"
                ],
    "envFlags":  [
                     "--env-file",
                     ".env.production"
                 ],
    "domain":  "fmcg.stratagemengine.com",
    "healthPath":  "/",
    "healthCodes":  [
                        "200"
                    ],
    "migrate":  null,
    "notes":  "CORRECTED 2026-09-14 (PR #1 review): .branch was wrongly set to master while every note below (and the live box) has always run feature/stratagem-integration-and-spec-consolidation -- a deploy.sh FMCG-Simulataor run would have reset origin/master onto production, discarding this branch's fixes. Fixed to match the branch actually running. PHASE 0 DONE 2026-09-12: /opt/apps/FMCG-Simulataor is now a real git checkout of origin/feature/stratagem-integration-and-spec-consolidation (commits 661c04d, ec40f98), verified with a clean --no-cache build of `backend` before cutover, old dir kept at FMCG-Simulataor.pre-git-backup-20260912. NODE_AUTH_TOKEN (read:packages) for the private @usamarasheed26/sdk package -- missing as of 2026-09-12, RESOLVED by 2026-09-13: the token is now present in the box's .env.production and `web` was rebuilt from source that day (fresh image build timestamp confirmed; deploy.sh run recorded as commit 63bae5f, success) -- web is no longer running a frozen pre-git image. DB/SECRETS FIX (661c04d/ec40f98) already covered docker-compose.prod.yml/Dockerfile.web/nginx.conf. NOT reconciled on purpose: docker-compose.yml (the DEV file)'s postgres service is commented out on the box -- that was a one-off safety edit to stop an orphaned container from being recreated in PRODUCTION (10-SEPT doc section 12 gap #4), not a fix that belongs in the actual local-dev compose file (a developer running this locally still needs a working local postgres) -- repo's docker-compose.yml correctly still has it active, left as-is. deploy MUST use docker-compose.prod.yml only, never docker-compose.override.yml (local-only dev cruft)."
}
