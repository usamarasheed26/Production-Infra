{
    "repo":  "usamarasheed26/ZeroToPMF",
    "branch":  "feature/stratagem-platform-integration",
    "compose":  [
                    "-f",
                    "docker-compose.prod.yml"
                ],
    "envFlags":  [
                     "--env-file",
                     ".env.production"
                 ],
    "domain":  "zerotopmf.stratagemengine.com",
    "healthPath":  "/",
    "healthCodes":  [
                        "200"
                    ],
    "migrate":  null,
    "notes":  "CORRECTED 2026-09-14 (PR #1 review): .branch was wrongly set to main while every note below (and the live box) has always run feature/stratagem-platform-integration -- a deploy.sh ZeroToPMF run would have reset origin/main onto production, discarding this branch's platform-integration fixes. Fixed to match the branch actually running. PHASE 0 DONE 2026-09-12: /opt/apps/ZeroToPMF is now a real git checkout of origin/feature/stratagem-platform-integration (commit bd639ab), verified with a clean --no-cache build of `api` before cutover, old dir kept at ZeroToPMF.pre-git-backup-20260912. NODE_AUTH_TOKEN (read:packages) for the private @usamarasheed26/sdk package -- missing as of 2026-09-12, RESOLVED by 2026-09-13: the token is now present in the box's .env.production and `web` was rebuilt from source that day (fresh image build timestamp confirmed; deploy.sh run recorded as commit cdfc894, success) -- web is no longer running a frozen pre-git image. All ~30 backend/frontend .py/.tsx files that looked different in a naive diff turned out to be pure CRLF/LF noise (box files are LF, repo's are CRLF) -- verified via a CR-stripped diff loop; only docker-compose.prod.yml/Dockerfile.web/.gitignore had real (already-known, already-committed) differences. Migration runs via the one-shot 'migrate' compose service (alembic upgrade head) that compose brings up before api -- no separate deploy-time migrate needed. Integrates with the platform via PLATFORM_VERIFY_SESSION_URL + SIM_API_KEY (backend/.env, box-only)."
}
