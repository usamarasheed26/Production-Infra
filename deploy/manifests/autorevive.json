{
  "repo": "usamarasheed26/autorevive-dynamics",
  "branch": "master",
  "compose": ["-f", "docker-compose.prod.yml"],
  "envFlags": ["--env-file", ".env.prod"],
  "domain": "autorevive.stratagemengine.com",
  "healthPath": "/",
  "healthCodes": ["200", "307", "308"],
  "migrate": "exec -T api alembic upgrade head",
  "notes": "UPDATED 2026-09-14 (PR #1 review + INFRASTRUCTURE_SOURCE_OF_TRUTH_2026-09-14.md audit): this manifest previously said 'PHASE 0 STILL BLOCKED (2026-09-12)' -- that is now stale. PHASE 0 COMPLETED 2026-09-13/14: /opt/apps/autorevive is a real git checkout of origin/master (HEAD 104fcc1), reconciled onto the shared authoritative Postgres (`autorevive` DB, no embedded db container), the `coolify` Docker network, and a correct Traefik label block (Host(`autorevive.stratagemengine.com`), tls, certresolver=letsencrypt, service port 80) -- the previously-blocking dev-shaped compose template described below was NOT used as-is; the compose file was properly reconciled first. A `deploy.sh autorevive` run succeeded 2026-09-14 03:02 (11s, recorded in /opt/apps/.deploy-history.log). The abandoned dev-shaped clone this manifest used to warn about is preserved for reference at /opt/apps/autorevive.pre-git-20260913 (renamed from the earlier autorevive.git-new), not deleted.\n\nSSO STATUS -- CHANGED, NEEDS FUNCTIONAL VERIFICATION: recent commits on master (bf53f16 'Stratagem platform telemetry, engagement engine, and facilitator integration', 1448367 'dynamic platform API resolution and authenticated facilitator bootstrap', e14d13f, 7af6a93) indicate real platform-SSO integration work landed, and a new `frontend/app/stratagem/page.tsx` route now exists -- this appears to supersede the 'zero platform-SSO integration' gap previously documented here. However, `.env.prod` still shows no PLATFORM_VERIFY_SESSION_URL/SIM_API_KEY pair the way ZeroToPMF's does (only STRATAGEM_NPM_TOKEN, which is a GitHub Packages auth token for installing the private @usamarasheed26/sdk, not a platform-session-verification credential), so the exact SSO mechanism and whether the old unconditional `redirect(\"/dashboard\")` in the root page was actually fixed is UNCONFIRMED -- do a live facilitator-launch test end to end before relying on this being resolved. Root frontend serves via an internal nginx container (app_autorevive), now correctly named (container_name namespacing fixed in commit 104fcc1, which also renamed the api/frontend containers to autorevive_api/autorevive_frontend). Redirects to a login-adjacent route by design (307/308). .env.prod is box-only, contains its own local JWT scheme values (ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, REFRESH_TOKEN_EXPIRE_DAYS) alongside the newer platform-integration pieces -- both appear to coexist currently."
}
